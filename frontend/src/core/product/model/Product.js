export class Product {
    constructor(id, name, unit, quantity, status) { 
        this.id = id;
        this.name = name;
        this.unit = unit;
        this.quantity = quantity; 
        this.status = status;
    }
}